# Context layer (Task D)

## Що покращив (один шар)

- [x] `app/AGENTS.md` — додав:
  - **стек:** TypeScript 5.x, ESM, Vitest;
  - **команди:** `npm install`, `npm test`, `npm run typecheck` (нотатка для PowerShell);
  - **конвенції:** integer cents; один файл на промпт; зміни через cookbook;
  - **layout:** `money.ts` (prod) vs `money.test.ts` (тести).
- [ ] ignore-файл — не змінював (у корені вже є `.cursorignore` з `node_modules`, `dist`, `.env`).
- **Чому саме цей шар:** агент у `app/` одразу знає стек, команди перевірки й scope «лише тестовий файл» — без читання всього репо (`materials/`, `docs/`, cookbook).

## Дія курації

- **Задача:** додати edge-case тести для `splitEvenly` у `app/src/money.test.ts`.
- **Що зробив (після):** вузький промпт з явним scope:
  ```
  Додай edge-case тести в @app/src/money.test.ts для splitEvenly.
  Дивись лише @app/src/money.ts і @app/AGENTS.md. Не чіпай інші файли.
  ```
- **Базовий варіант (до курації):** короткий промпт без `@file` і без обмеження scope:
  ```
  Додай тести для метода splitEvenly
  ```

## Вимірювання

| Метрика | До (без курації) | Після (з `@file` + scope) |
|---|---|---|
| Контекст / витрати сесії (токени, ≈) | 302 000 | 290 000 |
| Output токени за задачу (≈) | — | — |
| Ітерацій до результату | 1 | 1 |

**Різниця:** ≈ **12 000 токенів** (−4%) на тій самій задачі. **Вартість цієї різниці** при $0,5 за 1 млн токенів: 12 000 ÷ 1 000 000 × 0,5 ≈ **$0,006** (≈ **0,6 ¢**).

**Як міряв:** екран usage контексту в Cursor IDE (context tray) після кожного запиту в одній сесії.

**Результат задачі (обидва варіанти):** 9 тестів у `describe("splitEvenly")`, `npm test` — **24/24** green, змінений лише `money.test.ts`.

## Висновок

`app/AGENTS.md` + явні `@file` і заборона чіпати інші файли зменшили витрати контексту (~12k токенів) і зафіксували scope (лише `money.test.ts`). Якість однакова — 1 ітерація в обох випадках; виграш — менше шуму з репо й чіткіші межі для агента. Додатково `.cursorignore` у корені тримає `.env` і `node_modules` поза контекстом (зв’язок з Task B/C).
